# NixOS Config

My NixOS config.

```bash
sudo nixos-rebuild switch --flake .#will
```

```bash
mkdir ~/Drive
mkdir ~/Applications
mkdir ~/Emulation
mkdir ~/Emulation/ROMs
mkdir ~/Emulation/WADs
mkdir ~/Code
```
